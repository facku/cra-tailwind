import React from 'react';

const Button = () => {
	return (
		<button className="text-red-500 border-red-500 border p-3 focus:outline-none rounded">
			Boton
		</button>
	);
};

export default Button;
